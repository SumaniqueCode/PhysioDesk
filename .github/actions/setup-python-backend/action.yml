name: Setup backend Python
description: Install Python 3.12 with pip caching keyed on the backend project.

runs:
  using: composite
  steps:
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: pip
        cache-dependency-path: backend/pyproject.toml
