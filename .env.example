# PostgreSQL settings consumed by docker-compose.
# Copy to .env and adjust as needed. If port 5432 is taken by a local Postgres, change POSTGRES_PORT.
POSTGRES_USER=physiodesk
POSTGRES_PASSWORD=physiodesk
POSTGRES_DB=physiodesk
POSTGRES_PORT=5432
